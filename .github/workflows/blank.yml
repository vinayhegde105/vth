on: workflow_dispatch

name: Download git-tfs

jobs:
  download_windows:
    name: Downloading windows for tfs-git
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
        
      - name: test
        run: | 
          echo "party"
          choco install netfx-4.6.2-devpack
          
      - run: |
          (New-Object System.Net.WebClient).DownloadFile('https://get.scoop.sh', 'scoop.ps1') 
          ./visualstudio2013teamexplorer.ps1 -RunAsAdmin
        
        
        
        
        
      
