on: workflow_dispatch

name: Download git-tfs

jobs:
  download_windows:
    name: Downloading windows for tfs-git
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
          
      - run: |
            choco install visualstudio2013teamexplorer -y
        
        
        
        
        
      
